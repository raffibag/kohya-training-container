# == DO NOT INSTALL torch/torchvision/xformers here (in base already) ==

diffusers==0.27.2
transformers==4.40.2
accelerate==0.27.2
tokenizers==0.19.1
huggingface-hub==0.20.3
safetensors==0.4.2
peft==0.10.0
bitsandbytes==0.41.3
Pillow==10.2.0
opencv-python-headless==4.9.0.80
protobuf==4.23.4
einops==0.7.0
controlnet-aux==0.0.10
rich~=13.7
pyarrow==15.0.2
numpy==1.26.4

# numeric & image
scipy~=1.11
imagesize~=1.4
scikit-image~=0.22

# HF + data
datasets~=2.18
timm~=0.9
sentencepiece~=0.1

# OpenAI CLIP for image labeling
git+https://github.com/openai/CLIP.git

# runtime
fastapi~=0.110
uvicorn[standard]~=0.29
flask~=3.0
boto3~=1.34

# utils
regex~=2024.5
requests~=2.31
tqdm~=4.66
pyyaml~=6.0
omegaconf~=2.3
toml~=0.10
tomli~=2.0
ftfy~=6.1
voluptuous~=0.14

# logging & visualization
tensorboard~=2.15

# networkx (only if required): ~=3.2
# wandb (only if used): ~=0.16