# PyTorch already installed in base image with CUDA 11.8 support
# torch==2.1.2
# torchvision==0.16.2
opencv-python-headless
pillow
scipy
tqdm
imagesize
safetensors
transformers
accelerate
# Skip bitsandbytes for now - it pulls CUDA 12
# bitsandbytes
diffusers
datasets
omegaconf
peft
toml
gradio
altair
easygui
einops
# Skip pytorch-lightning - it pulls CUDA 12 deps
# pytorch-lightning  
tensorboard
wandb
networkx==2.8.8
voluptuous
flask
boto3
# Using ftfy and regex manually instead of git CLIP to avoid torch upgrades
ftfy
regex